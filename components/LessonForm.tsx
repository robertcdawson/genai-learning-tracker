"use client";import{useState}from"react";export default function LessonForm({onAdd}){const[title,setTitle]=useState("");function submit(e){e.preventDefault();if(!title.trim())return;const now=new Date().toISOString();onAdd({id:crypto.randomUUID(),title,status:"Todo",priority:3,tags:[],createdAt:now,updatedAt:now,reviewLevel:0});setTitle("")}return(<form onSubmit={submit} className="card"><input placeholder="New lesson…" value={title} onChange={e=>setTitle(e.target.value)}/><button type="submit">Add</button></form>)}